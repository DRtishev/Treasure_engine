# SNAPSHOT — EPOCH-BOOT

- Branch: `work`
- Objective: bootstrap deterministic, evidence-backed baseline run.
- Determinism controls observed: seed-defaulted gate wrappers, run-scoped artifact directories.
- Live execution status: no live orders enabled in bootstrap path; paper and dry-run paths used.
