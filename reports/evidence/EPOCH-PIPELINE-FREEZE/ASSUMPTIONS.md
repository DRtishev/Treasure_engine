# ASSUMPTIONS
- verify:* offline gates are runnable without external network calls by default.
- run_with_context keeps E2/Paper outputs under reports/runs scoped paths.
- current branch starts from prior autopilot commit and may contain hardcoded evidence epoch paths.
- FINAL_VALIDATED.zip can be rebuilt reproducibly from repo root excludes.
