# E134 BASELINE VERIFY
## CI=true CHAIN_MODE=FAST_PLUS QUIET=1 npm run -s verify:e131
```
exit:0
```
## CI=true CHAIN_MODE=FAST_PLUS QUIET=1 npm run -s verify:e132
```
exit:0
```
## CI=true CHAIN_MODE=FAST_PLUS QUIET=1 npm run -s verify:e133
```
exit:0
```
