#!/usr/bin/env node
import { spawnSync } from 'node:child_process'; import { enforceCIBoundaryE130, isCITruthy, snapshotState, writeMdAtomic } from './e130_lib.mjs';
enforceCIBoundaryE130(); const update=process.env.UPDATE_E130_EVIDENCE==='1'; if(update&&isCITruthy()) throw new Error('E130_CI_UPDATE_REJECTED'); const pp=['.foundation-seal/E130_INPUT_BINDING.json','.foundation-seal/runs','.foundation-seal/capsules','.foundation-seal/overlay','.foundation-seal/ledger']; const b=snapshotState(pp); const gb=spawnSync('git',['status','--porcelain'],{encoding:'utf8'}).stdout; const r=spawnSync('node',['scripts/verify/e130_evidence.mjs'],{stdio:'inherit',env:{...process.env}}); if((r.status??1)!==0){const a=snapshotState(pp); writeMdAtomic('reports/evidence/E130/ZERO_WRITES_ON_FAIL.md',['# E130 ZERO WRITES ON FAIL',`- status: ${b===a?'PASS':'FAIL'}`,'- reason: orchestrator_fail_path',`- state_before: ${b}`,`- state_after: ${a}`].join('\n')); process.exit(r.status??1);} const ga=spawnSync('git',['status','--porcelain'],{encoding:'utf8'}).stdout; if(!update&&gb!==ga) throw new Error('E130_READ_ONLY_VIOLATION');
