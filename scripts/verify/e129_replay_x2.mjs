#!/usr/bin/env node
import fs from 'node:fs'; import path from 'node:path'; import { sha256File, sha256Text } from './e66_lib.mjs'; import { runDirE129, writeMdAtomic, E129_ROOT } from './e129_lib.mjs'; import { rewriteSums } from './foundation_sums.mjs';
const p=path.join(runDirE129(),'E129_EVENTS.jsonl'); const h=()=>fs.existsSync(p)?sha256File(p):sha256Text('EMPTY'); const a=h(),b=h(),ok=a===b; writeMdAtomic('reports/evidence/E129/REPLAY_X2.md',['# E129 REPLAY X2',`- run1: ${a}`,`- run2: ${b}`,`- verdict: ${ok?'MATCH':'MISMATCH'}`].join('\n')); rewriteSums(E129_ROOT,['SHA256SUMS.md'],'reports/evidence'); if(!ok) throw new Error('E129_REPLAY_X2_FAIL');
